name: Deploy to cPanel

on:
  push:
    branches:
      - main  # Change this to your main branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to cPanel
        uses: wints/ftp-upload@v3
        with:
          server: ftp.issm.ai
          username: issmai
          password: 628KrISbn0uQ:[
          localDir: ./dist
          remoteDir: /public_html

